/* TODO: Need to setup logger. Most important to set the colors up
*   so it is comfortable to read logs
*   Here you can find documentation */
const {logger} = require('react-native-logs');
const log = logger.createLogger();

module.exports = {
    logger: log
}